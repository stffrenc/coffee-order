spring:
  datasource:
    url: jdbc:postgresql://localhost:6002/coffee
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always